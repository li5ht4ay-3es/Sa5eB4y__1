on:
  workflow_dispatch:
  schedule:
    - cron: '0 5 * * *'

env:
  pa5: ${{ secrets.pa5 }}

jobs:

  ac5io4s:
    runs-on: ubuntu-latest

    steps:
      - uses: li5ht4ay-3es/Sa5eB4y@ac5io4s
        with:
          token: ${{ secrets.pa5 }}
